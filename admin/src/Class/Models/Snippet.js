export default class Snippet {
  constructor(id, name, description, code, type, categoryId) {
    this.id = id
    this.name = name
    this.description = description
    this.code = code
    this.type = type
    this.categoryId = categoryId
  }
}
